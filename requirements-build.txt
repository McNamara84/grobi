# Build dependencies for creating Windows EXE with Nuitka
# Install with: pip install -r requirements-build.txt
# Version constraints keep major upgrades explicit while allowing patch updates

nuitka>=4.1.2,<5.0.0
ordered-set>=4.1.0,<5.0.0
zstandard>=0.25.0,<1.0.0
